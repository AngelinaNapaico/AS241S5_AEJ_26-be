<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.3.5</version>
		<relativePath/>
	</parent>
	<groupId>ap1</groupId>
	<artifactId>angelina.napaico</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>angelina.napaico</name>
	<description>Spring WebFlux + MongoDB consuming YouTube138 and Google Translate113 APIs</description>

	<properties>
		<java.version>17</java.version>
		<!-- Forzar versiones de Netty para corregir CVEs HIGH -->
		<netty.version>4.1.133.Final</netty.version>
		<!-- Forzar versión de Spring Framework para corregir CVEs HIGH -->
		<spring-framework.version>6.2.18</spring-framework.version>
		<!-- Forzar versión de Logback para corregir CVEs MEDIUM/LOW -->
		<logback.version>1.5.25</logback.version>
		<!-- Forzar versión de Jackson para corregir CVEs MEDIUM -->
		<jackson-bom.version>2.18.6</jackson-bom.version>
		<!-- Forzar versión de Reactor Netty para corregir CVEs MEDIUM -->
		<reactor-bom.version>2024.0.8</reactor-bom.version>
	</properties>

	<dependencies>
		<!-- WebFlux reactivo -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webflux</artifactId>
		</dependency>

		<!-- MongoDB reactivo -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-mongodb-reactive</artifactId>
		</dependency>

		<!-- Lombok -->
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>

		<!-- Test -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>io.projectreactor</groupId>
			<artifactId>reactor-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<annotationProcessorPaths>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
